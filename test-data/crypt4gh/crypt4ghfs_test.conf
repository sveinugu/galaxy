# Minimal crypt4ghfs configuration for unit tests.
# Galaxy injects "rootdir" and "extension" per-dataset at runtime;
# this file only needs to carry the key settings.
[CRYPT4GH]
seckey = compute_key.sec

[FUSE]
options = ro,default_permissions
